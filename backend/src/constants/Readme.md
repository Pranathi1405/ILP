# Constants Directory

Contains reusable application-wide constants.

Examples:
- HTTP status codes
- Error codes
- User roles
- Course statuses
- Payment statuses

Purpose:
Avoid magic strings and hardcoded values across the project.