# Models Directory

Contains database models.

Each model:
- Represents a database table
- Handles database queries
- Defines relationships
- Contains static query methods

Purpose:
Acts as the data access layer between services and the database.